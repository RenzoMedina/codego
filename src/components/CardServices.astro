---
interface Props {
  title: string;
  content: string[];
}
const {title, content} = Astro.props
---
<div class="card-services">
    <p>{title}</p>
    <ul>
        {content.map((item) => (
            <li>{item}</li>
        ))}
    </ul>
</div>
<style>

</style>